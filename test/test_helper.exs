ExUnit.start()
Ecto.Adapters.SQL.Sandbox.mode(Foyer.Repo, :manual)

# Mox mocks are defined here — one per port behaviour.
# When a new context is added, add its mock here:
#
#   Mox.defmock(Foyer.SomeMock, for: Foyer.SomePort)
#
# See docs/TESTING_GUIDE.md for the full pattern.
