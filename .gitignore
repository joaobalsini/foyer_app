# --- Elixir / Mix ------------------------------------------------------------
/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/
.lexical/

# Hex package tarballs (mix hex.build)
foyer-*.tar

# Dialyzer
/priv/plts/*.plt
/priv/plts/*.plt.hash

# --- Phoenix -----------------------------------------------------------------
# Compiled assets generated by esbuild/tailwind via Mix
/priv/static/assets/
/priv/static/cache_manifest.json

# Static binaries fetched by esbuild/tailwind Mix tasks
/_build/esbuild
/_build/tailwind

# Uploaded files in dev
/priv/static/uploads/

# --- Node / npm (used for asset tooling) -------------------------------------
/assets/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Nix / direnv ------------------------------------------------------------
/.direnv/
/.nix-mix/
/.nix-hex/
result
result-*

# Private direnv overrides — see .envrc.
.envrc.local

# --- macOS / editors ---------------------------------------------------------
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# --- Local env / secrets -----------------------------------------------------
# Env vars live in `.envrc` (committed, with fake placeholders) and
# `.envrc.local` (private, never commit).
.env
.env.*

# --- Test artifacts ----------------------------------------------------------
/tmp/
.claude/
_build
deps

## Other

.postgres/*
.keep